#include <string.h>
#include "../AFD/afd.h"

AFD *complementacao(AFD *entrada){

    AFD *ent = entrada;
    AFD *comp = *ent;

    for(int i; i<ent->qtdEstFinal; i++){
        if(ent->estados[])
    }
}